.PDF_extractor.py (and compiled .exe) is used to extract all the pages from each PDF in the current working directory (wherever the .exe is deposited) as a separate .pdf file.

Its purpose was in support of miscellaneous referencing and archiving activities.